import React  from 'react';
import { Link } from 'react-router';

export var Nav = React.createClass({
    render: function () {
        return (
            <div>
            </div>
        );
    }
});